/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // experimental: {
  //   appDir: false, // Удалите или закомментируйте эту строку
  // },
};

module.exports = nextConfig;